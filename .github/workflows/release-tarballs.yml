name: Release tarballs

on:
  push:
    tags:
      - "*"
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  build:
    name: Build ray-env tarball
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build
        env:
          DOCKER_BUILDKIT: "1"
        run: |
          make build

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ray-env.tar.gz
          path: ray-env.tar.gz
          if-no-files-found: error
          retention-days: 7
